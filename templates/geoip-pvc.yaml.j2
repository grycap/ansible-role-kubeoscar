apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geoip-db
  namespace: {{ monitoring_namespace | default('monitoring') }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: {{ volume_storage_class_name }}
